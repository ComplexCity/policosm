"""Read and write graphs in shapefile format.

shapefile saves a graph as geolocalized lines in a shapefile.

The matrix is symmetrical if for an undirected graph.

This implementation does not support mixed graphs (directed and
undirected edges together).

Format
------
Shapefile is described in this 1998 document https://www.esri.com/library/whitepapers/pdfs/shapefile.pdf
"""

